# packetlens
PacketLens - A lightweight, portable proxy tool that attaches only to the browser session it launches. Perfect for debugging in an isolated environment without changing system-wide settings.
